<nav class="nav main-nav has-shadow">
  <div class="nav-left">
    <a class="nav-item">
      {{#fa-stack size="lg" as |s|}}
        {{s.stack-2x "circle" class="bottom-icon"}}
        {{s.stack-1x "fa-paper-plane-o" class="top-icon"}}
      {{/fa-stack}}
      <h1 class="branding">TargetedLeads</h1>
    </a>
  </div>

  <!-- This "nav-toggle" hamburger menu is only visible on mobile -->
  <!-- You need JavaScript to toggle the "is-active" class on "nav-menu" -->
  <span class="nav-toggle">
    <span></span>
    <span></span>
    <span></span>
  </span>

  <!-- This "nav-menu" is hidden on mobile -->
  <!-- Add the modifier "is-active" to display it on mobile -->
  <div class="nav-right nav-menu">
    
    {{#link-to 'index' class="nav-item" activeClass="is-active"}}Message Triggers{{/link-to}}
    {{#link-to 'account' class="nav-item" activeClass="is-active"}}{{fa-icon 'cogs'}}{{/link-to}}

    <span class="nav-item">
      <a class="button is-primary">
        {{fa-icon 'sign-out'}} Sign out
      </a>
    </span>
  </div>
</nav>

<div class="container">
  {{outlet}}
</div>